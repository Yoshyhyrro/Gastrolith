pl1-hlasm-generator

Minimal scaffold to generate HLASM from PL/I using a parser.

Build (requires dune and opam-installed libraries):

```powershell
opam install dune angstrom
dune build
```

Run (after build):

```powershell
_dune/default/bin/pl1_hlasm_generator.exe
```
