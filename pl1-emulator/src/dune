;; Dune file for pl1-emulator/src

(executables
 (public_names pl1_parser)
 (names main)
 (libraries menhirLib)
 ;; Prefer native mode (ocamlopt)
 (modes native))

(menhir
 (modules parser))

(rule
 (targets lexer.ml)
 (deps lexer.mll)
 (action (run ocamllex %{deps})))
